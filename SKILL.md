---
name: student-assistant
description: |
  大学生全能助手 v1.1 — 课表、作业、考试、复习、成长、目标、简历、规划、
  仪表盘、Excel、PPT、通知、OCR、iCal。触发词：课表 | 作业 | 考试 | 复习 |
  打卡 | 目标 | 简历 | 规划 | 仪表盘 | 导出 | PPT | 通知 | 识别课表
---

# 大学生全能助手

> 核心原则：先判断用户意图 → 匹配命令 → 执行 → 展示结果 → 给下一步建议

## 01 / Name — 能力标识

`student-assistant` — 大学生全能助手

## 02 / Description — 何时加载

用户提到以下任一关键词时加载：课表、作业、考试、复习、打卡、目标、简历、规划、仪表盘、导出、PPT、通知、识别课表、今天有什么课、考试倒计时、今日提醒、成长记录、学期规划

## 03 / Goal — 解决什么，不解决什么

**解决**：
- 课表导入、查看、今日课程
- 作业添加、查看、标记完成、紧急提醒
- 考试添加、倒计时、临近提醒
- 期末复习：知识点清单、刷题追踪、错题本
- 每日成长：打卡、心情日记、连续统计
- 目标管理：长/短期目标、里程碑、OKR
- 简历生成：交互式填写、3种模板、输出 docx
- 大学规划：学期/周/日/四年规划
- 数据仪表盘：Web 可视化图表
- Excel 导出、PPT 生成、iCal 导出、桌面通知、OCR 识别

**不解决**：
- 不登录学校教务系统自动获取课表
- 不替用户做最终决策（选 offer、选导师）
- 不编造学校政策、考试日期等不确定信息

## 04 / Workflow — 执行顺序

```
用户表达需求
  → 匹配场景速查表
  → 执行对应命令（engine.py / 独立脚本）
  → 展示结果
  → 给出 1 个下一步建议
```

## 05 / Rules — 必须遵守的判断

1. **先初始化**：首次使用必须先 `init`
2. **命令优先**：优先调用脚本，不自己编造数据
3. **展示后给建议**：每次执行完附带下一步建议
4. **不编造信息**：课表/作业/考试以用户提供或存储数据为准
5. **保护隐私**：涉及真实个人信息时提醒脱敏

## 06 / Output — 怎样才算交付完成

- 课表：按天分组，时间排序，含课程/教师/地点/周次
- 作业：按截止日期排序，🔴🟡🟢 标识紧急度
- 考试：可视化进度条 `▓▓▓░░░`
- 提醒：课程+作业+考试三板块汇总
- 简历：输出 .docx，3 种模板可选
- 规划：结构化输出，可标记完成
- 仪表盘：Web 端可视化图表
- Excel：7 个 Sheet，含图表
- PPT：6 页结构（封面/目录/内容/总结）

## 07 / Gotchas — 常见陷阱

- 未初始化时执行其他命令 → 提示数据为空（正常）
- OCR 识别后不加确认 → 必须让用户确认再导入
- Web 服务启动后关闭终端 → 服务停止
- 通知功能无桌面环境 → 降级为控制台输出
- 简历生成无 python-docx → 提示安装依赖

## 08 / References — 细节去哪里按需读取

| 文档 | 内容 | 何时加载 |
|------|------|----------|
| `references/schedule-guide.md` | 课表导入格式（JSON/CSV） | 用户导入课表遇到格式问题时 |
| `scripts/engine.py` | 核心引擎，所有 CLI 命令 | 执行命令时 |
| `scripts/generate_resume.py` | 简历生成，3 种模板 | 用户说"生成简历"时 |
| `scripts/generate_ppt.py` | PPT 生成，3 种模板 | 用户说"做PPT"时 |
| `scripts/export_excel.py` | Excel 导出，7 个 Sheet | 用户说"导出Excel"时 |
| `scripts/notifier.py` | 桌面通知服务 | 用户说"启动通知"时 |
| `scripts/ocr_schedule.py` | OCR 识别课表截图 | 用户上传课表截图时 |
| `server.py` | Flask Web 服务 | 用户说"打开网页"时 |
| `index.html` | Web 可视化界面 | Web 服务启动后 |
